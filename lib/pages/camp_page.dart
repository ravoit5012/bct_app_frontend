import 'package:flutter/material.dart';

class CampPage extends StatelessWidget {
  const CampPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Camp Page'));
  }
}
